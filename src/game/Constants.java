package game;

public final class Constants {
    private Constants() {
        // no need for constructor
    }

    /*
     * gamePanel.mouseX > 650 && gamePanel.mouseX < 750 &&
     * gamePanel.mouseY > 400 && gamePanel.mouseY < 500 &&
     * gamePanel.mouseIsPressed && scoreCounter >= 300
     */
    // tower buttons
    private static final int TOWERBUTTONWIDTH = 100;
    private static final int TOWERBUTTONHEIGHT = 100;
    private static final int SUNBUTTONX = 650;
    private static final int SUNBUTTONY = 400;

}
